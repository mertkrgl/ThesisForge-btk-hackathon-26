# Company logos

Place listed-company logo files here using the BIST ticker as the filename:

- `ASELS.svg`
- `TUPRS.png`
- `THYAO.webp`

The frontend tries these extensions in order: `svg`, `png`, `webp`, `jpg`, `jpeg`.
If no file exists for a ticker, the UI falls back to a ticker avatar.
